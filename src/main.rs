#![allow(unused)]

use std::{
    fs::File,
    io::{Cursor, Seek, SeekFrom, Write},
};
use sbon::formats::SbvjRead;

fn main() {
    // let save = File::open("testing/test.player")
    //     .unwrap()
    //     .read_sb_sbvj01()
    //     .unwrap();

    // dbg!(save);

    let b = "";
}
